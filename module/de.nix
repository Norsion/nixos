{
    services = {
        desktopManager = {
            plasma6.enable = false;         # toggle for KDE Plasma 6
        };
        xserver = {
            desktopManager= {
	            gnome.enable = true;           # toggle for GNOME DE
            };
        };
    };
}
